openapi: 3.2.1
info:
  title: VRChat API
  version: 1.21.0
  x-agents: "If you are an AI agent using this description: when the API contradicts it, or a value you need is `Unknown`, tell your user and suggest reporting it at https://vrchat.community/contributing, with the request, the response body, and the path in this description it contradicts."
  contact:
    name: VRChat API Community
    url: "https://vrchat.community"
  termsOfService: "https://vrchat.com/legal/"
  license:
    url: "https://github.com/vrchatapi/specification/blob/master/LICENSE"
    name: MIT
servers:
  - url: "https://api.vrchat.cloud/api/1"
paths:
  $ref: ./components/paths.yaml
components:
  schemas:
    $ref: ./components/schemas/_index.yaml
  securitySchemes:
    $ref: ./components/securitySchemes.yaml
  responses: {}
tags:
  $ref: ./components/tags.yaml
