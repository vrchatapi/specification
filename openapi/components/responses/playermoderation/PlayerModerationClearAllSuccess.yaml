description: Success response after e.g. clearing all player moderations.
content:
  application/json:
    examples:
      200 OK:
        value:
          success:
            message: OK
            status_code: 200
    schema:
      $ref: ../../schemas/Success.yaml
