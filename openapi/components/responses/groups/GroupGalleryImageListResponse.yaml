description: Returns a list of GroupGalleryImage objects, wrapped in a paginated object when `v` is `2`.
content:
  application/json:
    schema:
      oneOf:
        - type: array
          items:
            $ref: ../../schemas/GroupGalleryImage.yaml
        - $ref: ../../schemas/GroupGalleryImageList.yaml
