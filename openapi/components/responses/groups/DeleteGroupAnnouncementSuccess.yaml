content:
  application/json:
    examples:
      Deleted Group Announcement:
        value:
          success:
            message: Group announcement was cleared!
            status_code: 200
    schema:
      $ref: ../../schemas/Success.yaml
