content:
  application/json:
    examples:
      Initiate Transfer:
        value:
          success:
            message: Group transfer cancelled.
            status_code: 200
    schema:
      $ref: ../../schemas/Success.yaml
