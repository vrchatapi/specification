content:
  application/json:
    examples:
      Blocked Group:
        value:
          success:
            message: Invite declined.
            status_code: 200
    schema:
      $ref: ../../schemas/Success.yaml
