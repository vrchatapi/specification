content:
  application/json:
    schema:
      title: GroupPostsResponse
      type: object
      properties:
        posts:
          type: array
          items:
            $ref: ../../schemas/GroupPost.yaml
        total:
          type: integer
