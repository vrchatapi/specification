content:
  application/json:
    schema:
      title: GroupMemberSearchResponse
      type: object
      properties:
        results:
          type: array
          items:
            $ref: ../../schemas/GroupMember.yaml
        total:
          type: integer
          description: Number of members returned
