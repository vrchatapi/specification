description: Error response when requesting the listings of another user.
content:
  application/json:
    examples:
      Access Denied:
        value:
          error:
            message: Access Denied
            status_code: 403
    schema:
      $ref: ../../schemas/Error.yaml
