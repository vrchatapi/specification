description: Error response when the seller's stores are unavailable to the caller.
content:
  application/json:
    examples:
      Stores Unavailable:
        value:
          error:
            message: Stores unavailable
            status_code: 403
    schema:
      $ref: ../../schemas/Error.yaml
