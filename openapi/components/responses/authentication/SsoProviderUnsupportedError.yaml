description: Error response when the requested SSO provider is unsupported.
content:
  application/json:
    examples:
      Unsupported Provider:
        value:
          error:
            message: '"That is not a supported SSO provider."'
            status_code: 400
    schema:
      $ref: ../../schemas/Error.yaml
