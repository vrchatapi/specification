description: Bad request error response when creating an instance
content:
  application/json:
    examples:
      Null Display Name:
        value:
          error:
            message: "displayName must be a string˸ 'null' - 'object'‚ displayName must be a string˸ 'null'"
            status_code: 400
    schema:
      $ref: ../../schemas/Error.yaml
