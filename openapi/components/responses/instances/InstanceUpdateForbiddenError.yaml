description: Error response due to not being allowed to update an instance
content:
  application/json:
    examples:
      Not Allowed to Update Instance Example:
        value:
          error:
            message: "You're not allowed to edit this instance․"
            status_code: 403
    schema:
      $ref: ../../schemas/Error.yaml
