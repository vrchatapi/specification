description: Error response when trying to respond to your own invite.
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          $ref: ../../schemas/Error.yaml
      required:
        - error
    examples:
      Cannot Respond Self Invite Error:
        value:
          error:
            message: "\"You cannot respond to your own invites.\""
            status_code: 400