description: Requesting your own ID returns your full `CurrentUser` record, carrying the private fields (`emailVerified`, `steamDetails`, `twoFactorAuthEnabled`, …) that never appear in responses for another user.
content:
  application/json:
    schema:
      title: UserResponse
      oneOf:
        - $ref: ../../schemas/User.yaml
        - $ref: ../../schemas/CurrentUser.yaml
