title: BoopRequest
type: object
description: See NotificationDetailBoop; either inventoryItemId (accessed through .id) by itself, or emojiId (accessed through .metadata.fileId) with optional emojiVersion
properties:
  emojiId:
    $ref: ../schemas/FileID.yaml
  emojiVersion:
    type: integer
  inventoryItemId:
    $ref: ../schemas/InventoryItemID.yaml
