title: UnavailableWorld
type: object
description: "Stands in for a world the API will not describe. `name` and `authorName` are `???`, `imageUrl` is empty, and the counts are `0`."
properties:
  authorName:
    type: string
    examples:
      - ???
  capacity:
    type: integer
  id:
    $ref: ./WorldID.yaml
  imageUrl:
    type: string
  isSecure:
    type: boolean
  name:
    type: string
    examples:
      - ???
  occupants:
    type: integer
  thumbnailImageUrl:
    type: string
    examples:
      - https://assets.vrchat.com/default/unavailable-world.png
required:
  - authorName
  - capacity
  - id
  - imageUrl
  - isSecure
  - name
  - occupants
  - thumbnailImageUrl
