title: UserNote
type: object
properties:
  createdAt:
    type: string
    format: date-time
  id:
    $ref: ./UserNoteID.yaml
  note:
    type: string
  targetUserId:
    $ref: ./UserID.yaml
  userId:
    $ref: ./UserID.yaml
required:
  - createdAt
  - id
  - note
  - targetUserId
  - userId
