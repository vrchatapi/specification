title: CalendarEventDiscovery
type: object
properties:
  nextCursor:
    type: string
    description: Pass back as the `nextCursor` query parameter to read the page after this one.
  results:
    type: array
    items:
      $ref: ./CalendarEvent.yaml
required:
  - results
