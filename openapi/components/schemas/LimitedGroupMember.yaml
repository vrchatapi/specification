title: LimitedGroupMember
type: object
properties:
  groupId:
    $ref: ./GroupID.yaml
  id:
    $ref: ./GroupMemberID.yaml
  isRepresenting:
    type: boolean
    description: Whether the user is representing the group in the world.
    example: false
  userId:
    $ref: ./UserID.yaml
