title: NotificationV2DataBoop
type: object
description: Either inventoryItemId by itself, or emojiId with optional emojiVersion
properties:
  emojiId:
    $ref: ../schemas/EmojiID.yaml
  emojiVersion:
    type: integer
  inventoryItemId:
    $ref: ../schemas/InventoryItemID.yaml
