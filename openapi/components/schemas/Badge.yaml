title: Badge
type: object
properties:
  assignedAt:
    type: string
    description: only present in CurrentUser badges
    nullable: true
    format: date-time
  badgeDescription:
    type: string
  badgeId:
    $ref: ./BadgeID.yaml
  badgeImageUrl:
    type: string
    description: direct url to image
  badgeName:
    type: string
  hidden:
    type: boolean
    description: only present in CurrentUser badges
    nullable: true
  showcased:
    type: boolean
  updatedAt:
    type: string
    description: only present in CurrentUser badges
    nullable: true
    format: date-time
required:
  - badgeDescription
  - badgeId
  - badgeImageUrl
  - badgeName
  - showcased
