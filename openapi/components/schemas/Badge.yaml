title: Badge
type: object
properties:
  assignedAt:
    type:
      - string
      - null
    format: date-time
    description: only present in CurrentUser badges
  badgeDescription:
    type: string
  badgeId:
    $ref: ./BadgeID.yaml
  badgeImageUrl:
    type: string
    description: direct url to image
  badgeName:
    type: string
  hidden:
    type:
      - boolean
      - null
    description: only present in CurrentUser badges
  showcased:
    type: boolean
  updatedAt:
    type:
      - string
      - null
    format: date-time
    description: only present in CurrentUser badges
required:
  - badgeDescription
  - badgeId
  - badgeImageUrl
  - badgeName
  - showcased
