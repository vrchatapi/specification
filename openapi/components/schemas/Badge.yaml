title: Badge
type: object
properties:
  assignedAt:
    type: ['null', 'string']
    description: only present in CurrentUser badges
    format: date-time
  badgeDescription:
    type: string
  badgeId:
    $ref: ./BadgeID.yaml
  badgeImageUrl:
    type: string
    description: direct url to image
  badgeName:
    type: string
  hidden:
    type: ['null', 'boolean']
    description: only present in CurrentUser badges
  isQuantifiable:
    type: boolean
  showcased:
    type: boolean
  updatedAt:
    type: ['null', 'string']
    description: only present in CurrentUser badges
    format: date-time
required:
  - badgeDescription
  - badgeId
  - badgeImageUrl
  - badgeName
  - showcased
