title: WebsocketFriendLocation
type: object
properties:
  canRequestInvite:
    type: boolean
  location:
    $ref: ./LocationID.yaml
  travelingToLocation:
    $ref: ./LocationID.yaml
  user:
    $ref: ./User.yaml
  userId:
    $ref: ./UserID.yaml
  worldId:
    $ref: ./WorldID.yaml
required:
  - canRequestInvite
  - location
  - travelingToLocation
  - user
  - userId
  - worldId
