title: InfoPushDataClickable
type: object
properties:
  parameters:
    type: array
    description: "In case of OpenURL, this would contain the link."
    items:
      type: string
  command:
    type: string
    enum:
      - CannedWorldSearch
      - OpenSafetyMenu
      - OpenURL
      - OpenVRCPlusMenu
    example: OpenURL
    minLength: 1
required:
  - command
