type: object
title: InfoPushDataClickable
properties:
  command:
    type: string
    minLength: 1
    examples:
      - OpenURL
    enum:
      - OpenURL
      - OpenVRCPlusMenu
      - OpenSafetyMenu
      - CannedWorldSearch
  parameters:
    description: 'In case of OpenURL, this would contain the link.'
    type: array
    items:
      type: string
required:
  - command