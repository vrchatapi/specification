title: TwoFactorRequired
type: object
properties:
  requiresTwoFactorAuth:
    type: array
    items:
      type: string
      enum:
        - totp
        - otp
        - emailOtp
required:
  - requiresTwoFactorAuth
