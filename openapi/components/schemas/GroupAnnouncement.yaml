title: GroupAnnouncement
type: object
description: >-
  An announcement is stored as a group post, so `POST /groups/{groupId}/announcement`
  answers with the post fields below as well as the announcement ones.
properties:
  authorId:
    $ref: ./UserID.yaml
    nullable: true
  createdAt:
    type: ['null', 'string']
    format: date-time
  editorId:
    $ref: ./UserID.yaml
    nullable: true
  groupId:
    $ref: ./GroupID.yaml
    nullable: true
  id:
    $ref: ./GroupAnnouncementID.yaml
    nullable: true
  imageId:
    $ref: ./FileID.yaml
    nullable: true
  imageUrl:
    type: ['null', 'string']
  roleIds:
    $ref: ./GroupRoleIDList.yaml
  text:
    type: ['null', 'string']
  title:
    type: ['null', 'string']
  updatedAt:
    type: ['null', 'string']
    format: date-time
  visibility:
    type: string
