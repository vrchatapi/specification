title: BareError
type: object
description: >-
  An error body carrying only a message string. Unlike `Error`, there is no
  nested object and no `status_code`, so a consumer that assumes the usual
  shape will read `undefined` from it.
properties:
  error:
    type: string
    example: You're not who I was expecting!
required:
  - error
